services:
  - mysql
language: elixir
elixir:
  - 1.5.2
otp_release:
  - 19.3
before_script:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get
  - mix deps.compile
  - MIX_ENV=test mix ecto.create
  - MIX_ENV=test mix ecto.migrate
script:
  - mix test
