sudo: true
before_install:
  -  chmod +x ./scripts/*
  - ./travis_mysql_5.7.sh
  - ./mysql-reset-root-password.sh
language: elixir
elixir:
  - 1.5.2
otp_release:
  - 19.3
before_script:
  - mix local.hex --force
  - mix deps.get
  - mix deps.compile
  - mix ecto.create
script:
  - mix test
